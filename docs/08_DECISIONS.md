# Decisions

## 2026-09-02 — Payroll moved out of MVP
**Decision:** Full Intelligent Payroll (calculation) is Phase 2 Product, not MVP. MVP does not include payroll.
**Why:** Discovery notes had listed payroll as confirmed MVP scope from HR, but the PRD roadmap places it in Phase 2 and defers "which payroll capabilities are essential to the first demonstrable version" to technical discovery. Emmanuel resolved to follow the PRD roadmap.
**Impact:** Consistent with the existing 9-phase dev plan, which already placed payroll last. See `01_REQUIREMENTS.md` §9.

## 2026-09-02 — HR Administrator / System Administrator scope clarified
**Decision:** HR Administrator is the system's primary operator persona — Business DNA setup, org/employee management, critical-role configuration/approval, workflow admin — not a fourth "dashboard" alongside Employee/Manager/Executive. Manager dashboard = department-level view. Executive dashboard = org-wide view across all departments/managers. System Administrator remains thin/unconfirmed for MVP.
**Why:** Discovery notes' Figma-coverage summary only mentioned 3 dashboards, which read as a gap against the PRD's 5 personas. Emmanuel clarified HR Administrator's screens are the setup/admin screens already covered, not a missing dashboard.
**Impact:** No new dashboard design needed for HR Administrator. See `01_REQUIREMENTS.md` "User Roles".

## 2026-09-02 — Critical Role Intelligence MVP scope locked in
**Decision:** MVP ships critical-role **classification/tagging only** (AI suggests, HR Administrator approves/edits/rejects). Vacancy flagging and any recruitment-side integration are deferred beyond MVP.
**Why:** Confirmed with Emmanuel. Classification has no cross-product dependency and is achievable within the 9-week timeline; vacancy flagging depends on classification existing first.
**Impact:** Closes the open item in `01_REQUIREMENTS.md` §4 — no longer pending.

## 2026-09-02 — AI-matched candidate suggestion for vacant critical roles: Phase 3+ vision, not MVP/Phase 2
**Decision:** Deferred beyond Phase 2, logged as a Phase 3+ vision item, not committed to any near-term roadmap slot.
**Idea:** When a critical role goes vacant, instead of (or ahead of) running job ads, the system pulls candidate matches from Elevare's existing recruitment platform's AI Talent Match engine and surfaces strong-fit candidates directly.
**Why deferred:**
- Depends on critical-role classification being built and stable first (see decision above) — that itself just landed.
- It's a cross-product integration between Workforce OS and the separate recruitment platform (different tenants/auth), not a feature toggle — needs its own API contract, data-sharing, and privacy design.
- PRD already places plain "Recruitment integration" in Phase 3 (its furthest-out tier); AI-ranked cross-platform matching is more ambitious than that, not less.
- Team is 2 people on a 9-week timeline to Nov 2026 — every addition now is a direct subtraction from what ships.
**Why worth keeping on the roadmap:** the recruitment platform already has AI talent-matching work in progress (`ai-talent-match-monetization.md`, `ai-talent-match-relevance-fix.md` in that codebase) — so this is a wiring/integration project later, not a from-scratch build. Strong differentiator once the foundation (classification, then vacancy flagging) exists.
**Impact:** Added to `01_REQUIREMENTS.md` "Explicitly Out of MVP Scope" under a new Phase 3+ vision note.

## 2026-09-02 — Backend framework: FastAPI, not Django
**Decision:** FastAPI, SQLAlchemy, Alembic, Celery — same stack as Elevare's existing recruitment platform.
**Why:** Emmanuel has current, recent production FastAPI experience (multiple prior projects) and would need to refresh Django. The recruitment platform already runs FastAPI + SQLAlchemy + Alembic + Celery + asyncpg in production, with real incidents already debugged there (Celery worker OOM, Alembic multiple-heads) — meaning the usual "Django ships faster, batteries included" argument doesn't hold when the alternative isn't a blank slate. Django's admin-panel advantage is also weaker here since the product ships a dedicated React frontend, not Django's HTML admin as product UI (SQLAdmin, built on the same SQLAlchemy models, covers the internal-ops-console use case reasonably well instead). Django's async ORM is also a `sync_to_async` shim over a synchronous driver, not real non-blocking I/O like SQLAlchemy `AsyncSession` + `asyncpg` — a real (if not urgent-for-MVP) difference for later features like cross-platform recruitment integration and concurrent AI calls.
**Impact:** `00_PROJECT_CONTEXT.md` tech stack table updated. Sole backend engineer now maintains one stack across both Elevare products.

## 2026-09-02 — Payroll reinstated to MVP (supersedes earlier deferral)
**Decision:** Payroll ships at MVP, in two layers: standard payroll (base salary, statutory deductions, attendance/leave adjustments) and incentive/bonus pay (per-role configurable, revenue-linked).
**Why:** Jennifer confirmed directly, in the 2026-09-02 stakeholder meeting, that payroll is required at MVP — overriding the earlier decision that had followed the PRD roadmap's Phase 2 placement. Emmanuel chose to follow the product owner's direct instruction rather than the roadmap document.
**Impact:** `01_REQUIREMENTS.md` §9 rewritten with both payroll layers, Nigerian statutory rates (sourced, pending accountant sign-off), and payroll architecture principles (immutable runs, effective-dated rates, decimal arithmetic, draft→review→approve→commit→lock, calculation separate from disbursement). Sequencing unchanged — payroll is still built last within MVP since it depends on the KPI/attendance/leave data the rest of the pipeline produces.

## 2026-09-02 — Incentive pay: three selectable models proposed, formula pending Jennifer
**Decision:** Rather than hardcode Jennifer's verbally-described bonus formula (which was ambiguous in the meeting transcript), propose supporting three industry-standard incentive models as configuration options per Position: (1) fixed amount per outcome, (2) percentage of value generated, (3) target-based bonus. Category (3) matches what Jennifer described. Message sent to Jennifer proposing this approach and requesting a worked numeric example (real salary/target/achievement/payout figures) to pin down the exact formula inside model 3.
**Why:** The transcript's description of the bonus math (allocated 40%, achieved 120%, "times 3") is genuinely ambiguous, and guessing at payroll math risks wrong payslips. Framing it as "which of these three models" rather than "please re-explain" gets a faster, more precise answer, and builds something reusable for future customers with different compensation structures, not just a one-off formula for Elevare.
**Status:** Open — waiting on Jennifer's reply. Do not build Layer B until the worked example arrives.
**Impact:** `01_REQUIREMENTS.md` §9 Layer B describes all three models; exact formula and per-role UI left unspecified pending her answer.

## 2026-09-02 — Stakeholder meeting corrections (Jennifer, Emmanuel, Uche)
A batch of smaller corrections surfaced in the 2026-09-02 meeting and the subsequent Journey Walkthrough deck review, all resolved same-day:

- **Critical Role model confirmed two-level:** department gets revenue allocation (example: Operations), position gets the criticality flag (example: General Manager). Criticality attaches to the role, not the person. See `01_REQUIREMENTS.md` §4.
- **Pipeline order corrected:** Business DNA → OKRs → **Critical Roles → KPIs** (not OKRs → KPIs → Critical Roles as originally documented). Jennifer was explicit that KPIs are built around already-identified critical roles/departments, not the reverse. This also corrects the onboarding wizard step order. See `00_PROJECT_CONTEXT.md` pipeline diagram and `01_REQUIREMENTS.md` workflow 1.
- **Objectives are additive:** mid-quarter or quarter-end objective additions append to existing KPIs/critical-role suggestions, never clear or replace them.
- **Quarterly Objective Review** added as a recurring workflow, tied to the org's configured fiscal year start.
- **Signup/identity model:** whoever signs up first becomes Organization Owner (business owner or HR, either can be first) — resolves Jennifer's correction that HR isn't necessarily first-in. Standard identity-vs-membership SaaS pattern, not a new persona.
- **Company name** moves from the registration form to the Business DNA onboarding step (duplicate field removed).
- **Microsoft/Entra SSO** deferred to Phase 2 (enterprise-tier), auth kept pluggable now.
- **Shift-based scheduling removed from MVP entirely** (not just renamed) — the Journey Walkthrough deck had built a full Morning/Evening/Night shift-assignment grid, which is more scope than intended. MVP attendance is plain clock-in/clock-out, matching Elevare's own case study as a standard office business. Full shift scheduling logged as a Phase 3 candidate.
- **Manager can add/invite employees directly** to their own department, without routing through the Org Owner.
- **Employee task list** only appears after clock-in, sortable by priority (High → Medium → Low).
- **Leave Management** promoted to its own explicit MVP module (request → approval → balance), previously only implied inside dashboard notes.
- **Executive dashboard:** quarterly default trend view (not six-month) plus custom date range; department rows drill down to individual employee performance.
- **Notifications are role-specific** (Employee/Manager/HR/Executive each see different streams) — confirmed as the requirement; whether the current single-page design already implements this is an open question sent to the frontend dev.

**Impact:** All folded into `00_PROJECT_CONTEXT.md` and `01_REQUIREMENTS.md`. Corresponding fix list sent to Uche (frontend dev) for the Journey Walkthrough deck; awaiting his updates.

## 2026-09-03 — Revenue-target mechanism clarified: it's OKR/KPI scoring, not payroll
**What happened:** A follow-up call transcript (Jennifer Otas, Uchechukwu Mbonuike) surfaced the same afternoon this decision log was last updated. In it, Jennifer explained the "40% allocated, 120% achieved, ×3" example from the 2026-09-02 meeting in full detail, unprompted. It turned out to describe a completely different mechanism than the one the 2026-09-02 decisions assumed.
**The actual mechanism:** Business DNA captures a capital investment figure (e.g. ₦25,000,000). That figure is allocated across critical departments as percentages (which can sum to over 100%, since each is an independent expected-return target, not a budget split) to produce each department's annual revenue target, divided by quarter. Individual tasks/KPIs carry a weight score (e.g. speed of closing a lead), and weighted performance rolls up to show whether a department/employee is tracking toward its revenue target, visualized as a trend graph. This is OKR/KPI generation and Performance Intelligence — it does not touch payroll or determine what anyone is paid.
**Why this matters:** the entire "payroll reinstated to MVP" chain of decisions from 2026-09-02, including the message sent to Jennifer proposing three incentive-pay models and requesting a worked payroll example, was built on interpreting this same example as a payroll bonus formula. It wasn't. Emmanuel confirmed directly (2026-09-03) that no clarifying question to Jennifer is needed on this point, and that the mechanism is purely for scoring/dashboards.
**Decision:** 
- The revenue-target/scoring mechanism is confirmed MVP scope, folded into Business DNA (§1), Critical Role Intelligence (§4), and Performance Intelligence (§7) in `01_REQUIREMENTS.md`.
- **Payroll reverts to Phase 2, not MVP** — see next entry. This corrects the 2026-09-02 reinstatement rather than adding a new decision on top of it.
- The three-incentive-models proposal and the "waiting on Jennifer's worked example" item are both closed. No message needs to be sent asking Jennifer to clarify payroll math — the thing that appeared to need clarifying wasn't payroll at all.
**Impact:** `00_PROJECT_CONTEXT.md` and `01_REQUIREMENTS.md` §1, §4, §7, §9 updated.

## 2026-09-03 — Payroll reverted to Phase 2 (corrects the 2026-09-02 reinstatement)
**Decision:** Payroll (both the standard/statutory layer and any incentive/bonus layer) is Phase 2 Product, not MVP. This reverses the 2026-09-02 "Payroll reinstated to MVP" decision.
**Why:** That reinstatement was based on believing Jennifer's revenue-allocation example was a payroll bonus formula. The 2026-09-03 follow-up call clarified it was describing OKR/KPI scoring instead (see prior entry). With that misreading corrected, no confirmed product-owner requirement for MVP payroll exists — the original PRD-roadmap placement (Phase 2) stands.
**What survives from the 2026-09-02 work:** the sourced Nigerian statutory rates (PAYE bands, pension, NHF, NSITF) and the payroll architecture principles (immutable runs, effective-dated rates, decimal arithmetic, draft→review→approve→commit→lock, calculation separate from disbursement) remain accurate and are kept in `01_REQUIREMENTS.md` §9 as reference material for whenever payroll is actually built.
**What does not survive:** the three-selectable-incentive-models proposal is not a scoped requirement — it was solving a problem that turned out not to exist in the form assumed. If a genuine payroll-incentive requirement surfaces later, it starts fresh, not from that proposal.
**What was unaffected at the time:** Leave Management (§10) stayed in MVP as of this entry — the request/approval/balance workflow doesn't depend on payroll calculation existing. (Superseded 2026-09-04 — see that entry below. Leave Management was separately moved to Phase 2, for unrelated reasons, not because of anything in this decision.)
**Impact:** `01_REQUIREMENTS.md` §9 rewritten with the history and reverted scope; Main Workflows and Out-of-Scope list updated to match; `00_PROJECT_CONTEXT.md` Important Constraints updated.

## 2026-09-04 — Leave Management moved to Phase 2 (not yet confirmed with Jennifer)
**Decision:** Leave Management (§10) moves out of MVP to Phase 2 Product.
**Why:** Emmanuel's own scope call while working through the database schema, to keep MVP focused on the Business DNA → OKR → Critical Role → KPI → daily execution loop. No new information from Jennifer drove this, unlike the payroll reversal, which was triggered by a clarifying call.
**Status:** Open — Jennifer explicitly asked for Leave Management in MVP during the 2026-09-02 meeting. This reversal has **not** been communicated to or confirmed by her yet. Treat as a proposal, not a settled fact, until that happens, the same way the payroll question was tracked before it was closed out.
**What survives:** the full request → approval → balance design in §10 (leave types, balances, manager approval flow) is kept as-is, ready to build in Phase 2, only the timing changed.
**Impact:** `01_REQUIREMENTS.md` §10, Main Workflow #7, and Out-of-Scope list updated. Database schema (`04_DATABASE.md` Cluster 7) proceeds with Attendance only, no leave tables, for MVP.

## 2026-09-04 — Multi-tenancy: shared schema, shared database, enforced by Row-Level Security
**Decision:** One Postgres database, one set of tables, every table carries a `NOT NULL organization_id`, isolation enforced at two independent layers: application-level authorization (role/permission checks within a tenant) and Postgres Row-Level Security policies (the tenant boundary itself, enforced by the database regardless of what the application queries for).
**Alternatives considered:**
- Schema-per-tenant (separate Postgres schema per customer) — rejected. Migrations run once per schema instead of once total, connection pooling (PgBouncer transaction mode) doesn't play well with per-request `search_path` switching, and cross-tenant analytics get much harder. A worse position on both axes than shared-schema, not a genuine middle ground.
- Database-per-tenant — rejected as the MVP default, kept as a future option. Strongest isolation, but highest operational cost (migrations, backups, and monitoring all multiply per tenant). This is the right answer for a specific enterprise contract that demands dedicated infrastructure (data residency law, noisy-neighbor elimination, independent audit/backup, or simply a procurement requirement), not for every customer by default. Real-world parallel: MongoDB Atlas sells exactly this spectrum as Shared vs. Dedicated cluster tiers.
**Why RLS specifically, not just application-level filtering:** application authorization checks ("is this user allowed to see this resource") remain necessary and are not replaced by RLS — they solve a different problem (in-tenant permissions, e.g. a Manager in one department shouldn't see another department's payroll even though it's the same organization). RLS solves the one thing application code can't be trusted to always get right: a `SELECT` that forgets its `WHERE organization_id = ...` clause, a raw script, a rushed admin tool, a future engineer unfamiliar with the convention. Postgres enforces the tenant boundary as an invisible policy on every query regardless of what the application asked for. This is the standard defense against IDOR-style cross-tenant leaks (Broken Access Control topped OWASP's 2021 Top 10), which is the single most common way multi-tenant SaaS actually gets breached in practice.
**Implementation notes:**
- `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` plus a policy checking `organization_id = current_setting('app.current_org_id')::uuid` on every tenant-scoped table.
- The current tenant is set once per request via `SET LOCAL app.current_org_id = ...`, inside a transaction, in a single FastAPI dependency, not scattered through the codebase. `SET LOCAL` (not plain `SET`) matters because pooled connections (PgBouncer) are reused across different tenants' requests — a session-scoped setting could otherwise leak between them.
- Elevare's own cross-tenant admin/analytics queries (e.g. billing status across customers) go through a separate, tightly controlled DB role permitted to bypass RLS — not the same connection path application requests use.
**Impact:** `00_PROJECT_CONTEXT.md` tech stack table updated from "leaning" to decided. This is now safe to build against in the database/system design pass.

## 2026-09-04 — Generic Workflow Template/Step model designed, closes the long-standing "Workflow Management" design gap
**Decision:** Workflows (the "Workflow & Departmental Interdependency" MVP feature, §6) are built as a fully generic engine, never as anything specific to Elevare's own process. Elevare's lead-intake process is just the first real data entered into the same tool every customer gets, not a feature built for Elevare specifically.
**Model:** WorkflowTemplate (name + ordered steps) → WorkflowStep (department, default turnaround time, position, optional default assignee) → WorkflowInstance (one running case) → Task (auto-created per active step, inherits deadline, completion triggers the next step). A department can appear more than once in one template. A step's task goes straight to its default assignee if one is set, otherwise lands with that department's manager to delegate. Deadlines are tracked as `due_at` vs `completed_at`; a task open past `due_at` flips to Overdue and notifies before it's even finished, not just after.
**Why this shape, and why it took several passes to land on:** the first drafts conflated things that needed separating, an external tool's data (a CRM) with Workforce OS's own task mechanism, a KPI-measured event (response time, no discrete task needed) with an actual assignable task, and a single hardcoded process with a genuinely reusable engine. Working through Elevare's own recruitment process, then a second, unrelated example (a retail company's complaint-resolution process, different departments, different step count, same engine, no code changes) is what confirmed the model is actually generic and not secretly Elevare-shaped.
**Permission:** template creation/editing is available to HR Administrator (org-wide) or any Manager. A manager building a workflow spanning other departments is expected to have already confirmed realistic turnaround times with those departments, same as they would without any software involved — no formal cross-department approval gate in MVP.
**Status:** Emmanuel proceeded with this model while flagging he doesn't yet have full confidence in every part of it. Revisit and refine once it's visible as an actual schema or screen, rather than treating this as unquestionable.
**Impact:** `01_REQUIREMENTS.md` §6 rewritten with the full model, cross-referenced to §7's weighted KPI scoring (same `due_at`/`completed_at` comparison feeds both).

## 2026-09-07 — Employee leaderboard, Company Performance Score, Most Improved, AI executive summary added to MVP
**Decision:** Four additions to the executive dashboard: an employee leaderboard, a headline Company Performance Score, a Most Improved employee/department callout, and a short LLM-generated executive summary per period.
**Why:** Two separate drivers. First, the leaderboard isn't really new, `01_REQUIREMENTS.md` §11 already required department drill-down to show "how each person's performance rolls up," confirmed with Jennifer 2026-09-02, but it was never actually modeled, there was no table capturing an individual employee's rolled-up score, only a department's. Second, Emmanuel raised that this MVP will be demoed to investors and reputable public figures, not just used internally, so a few cheap, high-visual-impact additions that reuse data and infrastructure already being built were worth proposing now rather than after launch.
**Why these four specifically:** all reuse existing infrastructure rather than requiring new subsystems. Company Performance Score and Most Improved are aggregate/comparison queries over `kpi_scores`/`employee_kpi_scores`, no new storage. The executive summary reuses the LLM integration already being built for AI Suggestions (`06_AI_DESIGN.md`), generated periodically via Celery Beat and stored, not computed live on every dashboard load (too slow, too costly per view).
**Status:** Emmanuel's own addition, not yet communicated to Jennifer or Uche. Doesn't reverse or conflict with anything she's confirmed, but as a product-visible change it should still be mentioned to her and to Uche (for the actual screens) as a courtesy, not treated as silently settled the way a pure technical decision would be.
**Impact:** `01_REQUIREMENTS.md` §11 updated. `04_DATABASE.md` Cluster 5 gets `employee_kpi_scores` and `executive_summaries`. `02_SYSTEM_DESIGN.md` Performance module ownership updated. `05_API_DESIGN.md` gets the leaderboard and company-performance-summary endpoints, and separately, the previously-missing Audit Log endpoints get added in the same pass (caught during the 2026-09-06 review, unrelated to this decision but fixed alongside it).

## 2026-09-07 — RLS-bypass admin role given concrete protections
**Decision:** The cross-tenant admin role mentioned in the 2026-09-04 multi-tenancy decision (`BYPASSRLS`, used for Elevare's own billing/analytics queries across all customers) gets four concrete protections, previously just implied: a separate Postgres role never used by the main application, a credential stored separately from the app's own database credential, invocation only via internal scripts, never an HTTP endpoint, and every use logged to `audit_log`.
**Why now:** caught during the 2026-09-06 gap review, this role existed conceptually but nothing specified who could use it, where its credential lived, or whether its use was accountable, for the single highest-privilege access path in the system.
**Clarified alongside this:** `System Administrator` (Elevare's own platform role, the only one that ever needs this) is not the same thing as `HR Administrator` or `Business Executive` (customer-side roles, scoped to their own organization, fully served by ordinary RLS, no bypass involved). Worth stating plainly since it's an easy conflation, a customer's own admin dashboard is entirely unaffected by anything in this entry.
**Impact:** `07_SECURITY.md` gets a full section on this.

## 2026-09-07 — CORS and token storage decided
**Decision:** Explicit CORS origin allow-list (no wildcard), `allow_credentials=True`. Refresh token stored as an `httpOnly` cookie (`Secure` flag environment-conditional, true in prod, false in dev over plain HTTP). Access token in `localStorage`, its short 15-minute expiry bounds exposure if this app ever has an XSS bug.
**Why:** CORS was previously undocumented, a gap caught in the 2026-09-06 review, yet mechanically required for the React frontend to reach the API at all. Token storage follows the exact pattern already run in production on Elevare's recruitment platform, kept rather than changed since it's a proven, reasonable tradeoff for this team.
**Impact:** `03_ARCHITECTURE.md` Auth Tokens section and new CORS section.

## 2026-09-07 — Soft-deleting a department/position is blocked, not cascaded, while active references exist
**Decision:** Deleting a department or position fails with a named reason (e.g. "5 active employees still assigned") if it still has active employees, open/in-progress tasks, or a pending AI suggestion attached. No cascading delete, no silent allow.
**Why:** caught in the 2026-09-06 gap review as an undefined rule. Cascading was rejected, it risks quietly wiping or orphaning real employee records. Silently allowing it was rejected too, it would leave active work pointing at something that no longer coherently exists. Blocking with a specific reason is the safer default and doesn't require new cascade machinery.
**What's unaffected:** historical references (a completed task, closed KPI scores) already had this covered by the existing soft-delete convention, nothing changes there.
**Impact:** `01_REQUIREMENTS.md` §2, `04_DATABASE.md` Cluster 2 design notes.

## 2026-09-07 — AI Suggestion generation made idempotent against Celery retries
**Decision:** A partial unique index on `ai_suggestions` (`organization_id`, `suggestion_type`, `position_id`, `department_id`, `kpi_id`, `suggested_department_name`, `WHERE status = 'pending'`), paired with `INSERT ... ON CONFLICT DO NOTHING` in the generation code.
**Why:** caught in the 2026-09-06 gap review. Celery retries failed jobs by default; without this, a retried generation job could create a duplicate pending suggestion for the same target. A database-level constraint was chosen over an application-level check-then-insert because the latter has a race condition under concurrent execution, the database is the only place this guarantee actually holds.
**Impact:** `04_DATABASE.md` Cluster 4, `06_AI_DESIGN.md` new Idempotency section.

## 2026-09-07 — LLM provider decided (Claude), AI usage/cost tracking added
**Decision:** Claude is the LLM provider for AI Suggestions and executive summary generation, closing the "provider TBD" left open in `06_AI_DESIGN.md`. Every LLM call is metered: a new `ai_usage_log` table records token counts and estimated cost per call, written through a shared internal LLM-calling utility so both features log through one place rather than duplicating "how do I call Claude" code.
**Why:** raised directly, "should AI costs/usage be tracked, at least for admins?" Real concern for a metered, per-call-cost dependency in a multi-tenant product, without this Elevare has no visibility into what AI features actually cost to run, per customer or in aggregate, until a bill arrives.
**Access:** an org's own HR Administrator sees their own organization's usage via `GET /ai-usage`, ordinary RLS, no special access. Aggregate cost across every customer, for Elevare's own margin tracking, goes through the same internal admin scripts already built for billing/cross-tenant work, not a new access pattern.
**Impact:** `04_DATABASE.md` Cluster 4 gets `ai_usage_log`. `06_AI_DESIGN.md` gets a Cost and usage tracking section, provider TBD resolved. `05_API_DESIGN.md` gets `GET /ai-usage`.

## 2026-09-07 — Fiscal-year-start field added, closing an implementation-planning gap
**Decision:** `organizations.fiscal_year_start_month` (`SMALLINT`, 1–12, `DEFAULT 1`).
**Why:** caught while drafting `09_PROGRESS.md` M6/M7 — `01_REQUIREMENTS.md` §4 and Main Workflow #4 require the Quarterly Objective Review to trigger "at each quarter's end, per the org's configured fiscal year," named as a real feature in prose but never actually given a column anywhere in `04_DATABASE.md`.
**Placement:** `organizations`, not `business_dna`. This is operational/platform configuration, the same category as `subscription_status` already on that table, not business-identity content like vision or mission, an org's fiscal year isn't a strategic statement the way core values are.
**Why a default instead of forcing a choice at onboarding:** `DEFAULT 1` (January) gives every org standard calendar-year quarters automatically, satisfying the requirement for real rather than cutting it, while an org that genuinely runs a different fiscal year (e.g. Apr–Mar) can still set it explicitly, no new onboarding step forced on the common case.
**Impact:** `04_DATABASE.md` Cluster 1. `09_PROGRESS.md` M2 (field ships here) and M7 (the Quarterly Objective Review's Celery Beat trigger reads it) updated accordingly.

This closes out all six gaps from the 2026-09-06 review. The two minor items are now also resolved: `GET /kpis/{id}/scores` added to `05_API_DESIGN.md`, and a Redis-backed 60-second debounce added to AI Suggestion generation (`06_AI_DESIGN.md`), reusing Redis rather than adding new infrastructure.

## 2026-09-07 — Billing: manual for MVP, Paystack/Flutterwave, automated subscription billing deferred
**Decision:** MVP billing is manual. Elevare invoices a customer directly, they pay via a Paystack or Flutterwave payment link (same providers already assumed for payroll disbursement in `01_REQUIREMENTS.md` §9) or bank transfer, and Elevare staff records it by setting `organizations.subscription_status`/`subscription_expires_at` through the internal admin scripts already designed for the RLS-bypass role. No self-serve checkout, no automated recurring billing, no automated access enforcement based on these fields, all deferred to Phase 2.
**Why:** No SaaS company, regardless of size, builds its own payment processing, PCI compliance and recurring-billing logic (proration, dunning, invoicing) are deep, already-solved problems, always handled through a third-party platform. The open question was timing, not vendor. Most early-stage B2B SaaS companies handle their first cohort of customers manually rather than building automated self-serve billing before there's proven demand for self-serve signup at scale, and this MVP being demoed to investors and pilot customers doesn't change that, a functioning manual process is a normal, expected answer at this stage, not a gap.
**Vendor choice:** Paystack or Flutterwave over Stripe, both are established Nigerian fintechs with real NGN support and existing subscription/recurring-billing APIs for when Phase 2 automation is built; Stripe's direct Nigerian support is inconsistent. Reusing whichever gets picked for payroll disbursement avoids a second third-party integration.
**Impact:** `04_DATABASE.md` Cluster 1, `organizations` gets `subscription_status`/`subscription_expires_at`. Automated billing (self-serve checkout, metering, dunning) logged here as explicit Phase 2 scope, not built now.

**Addendum, same day:** payment history visibility for the customer is a real, separate small addition, not covered by the above. `billing_records` (Cluster 1) is a payment history log, populated manually alongside the fields above, distinct from actual invoice generation, which stays Paystack/Flutterwave's job, not built in-house. `GET /billing/history` (Owner/HR Administrator only) and a matching read-only frontend screen were added for this. No payment page, no checkout flow, this is visibility only.

## 2026-09-05 — AI suggestion generation: LLM API call, not a hand-curated rules table (technical implementation choice, does not need Jennifer's sign-off)
**Decision:** Critical-role and revenue-allocation suggestions are generated by calling an LLM (Claude/GPT/Azure OpenAI, provider TBD) with the org's existing department/position/industry context, not by matching against a hand-written table of title patterns per industry.
**Why the original "rules-based" framing needed revisiting:** `01_REQUIREMENTS.md` §8 confirms, as an actual product requirement from Jennifer, "AI suggests, human approves, nothing auto-activates." It does not require any specific implementation technique. "Rules/template-based, not ML" was Emmanuel's own technical framing underneath that requirement, chosen because training a custom ML model needs a proprietary dataset that doesn't exist. That framing missed a third option: calling an already-trained LLM via API needs no dataset at all, and avoids the real practical problem a hand-curated rules table would have created, someone, most likely Jennifer, would have had to author and maintain title-matching patterns for every industry this product might ever sell into. That's not a reasonable ask of the product owner, and isn't necessary.
**Whose call this is:** Emmanuel's, as tech lead. Jennifer confirmed the product behavior (suggest, approve, never auto-activate); how that behavior gets implemented, rules engine vs LLM call vs anything else, is a technical decision, not a business one. No message to Jennifer needed for this change.
**What stays true:** the human-approval requirement is unchanged. AI is still restricted to entities that already exist (§4), and every suggestion still goes through the same pending/approved/edited/rejected review lifecycle in `ai_suggestions`.
**New consequence surfaced by this decision:** approvals and corrections were always meant to become future training data (§8), but `ai_suggestions` as originally drafted only recorded what the AI proposed, not what HR actually decided after review. Fixed same day, see `04_DATABASE.md` Cluster 4, `reviewed_*` columns added mirroring each `suggested_*` column, populated on both approval and edit, so the AI-proposed vs human-decided pair is always available for later training use.
**Impact:** `04_DATABASE.md` Cluster 4 updated (`reviewed_*` columns). No change to `01_REQUIREMENTS.md` §8's product-level requirement; its "rules/template-based" implementation detail is superseded by this entry.

## 2026-09-06 — Audit Log added to MVP scope (technical/architectural call, does not need Jennifer's sign-off)
**Decision:** A dedicated Audit Log module and `audit_log` table are added to MVP, not deferred to Phase 2. Every domain module logs its mutating actions through it.
**Why this is Emmanuel's call, not a product-scope question:** like the RLS decision, this is a security/architecture control, not a feature that changes user-facing product behavior or business rules. It doesn't need product-owner confirmation the way Leave Management's phase did.
**Why now, not later, and why this is different from every other "moved to Phase 2" decision in this log:** every other deferral in this project (Leave Management, payroll) was a genuine feature that can be built later without cost, the requirement doesn't change shape by waiting. Audit logging is the opposite: a write that happens before the audit call exists has no history to recover afterward. Building it alongside the modules being written now is materially cheaper than retrofitting instrumentation into eight already-built modules later.
**Scope drawn deliberately narrow:** the capture mechanism (module calls Audit Log's service on every mutation) is MVP. A dedicated viewer screen for browsing recorded history is not bundled into this decision, that data isn't lost by building the screen later, so it doesn't carry the same urgency.
**Impact:** `02_SYSTEM_DESIGN.md` (new Audit Log module, infrastructure category alongside Notifications), `04_DATABASE.md` Cluster 9 (new `audit_log` table), `07_SECURITY.md` updated (this was previously listed as explicitly out of MVP scope, corrected here).

## 2026-09-10 — M1 reviewed, committed; `docs/` was being excluded from git
**Decision:** M1 (Foundation & Infrastructure Scaffolding) reviewed against `03_ARCHITECTURE.md`/`07_SECURITY.md` and accepted as complete. Reviewed: Dockerfile/compose (single image, three containers, matches the documented topology), `platform_admin` BYPASSRLS role script (isolated — no app container references it), RLS regression test suite (correctly asserts nothing yet, since M2 hasn't landed the first RLS-protected tables). Auth-related code in `dependencies.py` (`get_current_user`, `require_role`, etc.) is present but commented out — expected, that's M2 scope (User model, JWT handling), not a gap in M1.
**Bug found and fixed:** `.gitignore` had `docs/` in it, which would have silently excluded the entire project-memory folder from version control — direct contradiction of this workflow's "docs is the source of truth" rule. Removed. Also added `celerybeat-schedule` to `.gitignore` (Celery Beat's runtime state file, not source).
**Impact:** Repo initialized and first commit made (M1 scaffolding + docs, `.env`/`.env.admin` correctly excluded via `.env.*`). No `03_ARCHITECTURE.md`/`07_SECURITY.md` changes needed — implementation matched the spec.

## 2026-09-13 — `organizations.name` made nullable (M2 implementation)
**Decision:** `organizations.name` is nullable, overriding `04_DATABASE.md`'s original `NOT NULL`. `POST /auth/register` creates the organization row with no name at all.
**Why:** Registration is identity-only per the 2026-09-02 signup decision (name, work email, password — company name captured later, at the Business DNA onboarding step, M5). The org row still has to exist at registration, since the founding Owner's `Membership` references its id. There is no company name available at that point to satisfy `NOT NULL` without either putting a company-name field back on the registration form (rejected — contradicts the identity-only signup decision) or writing a placeholder value (rejected — a fake name is worse than an honest `NULL` pending onboarding).
**Impact:** `04_DATABASE.md` Cluster 1 SQL and design notes updated. Business DNA (M5) is responsible for setting the real name during onboarding; a `NULL` name before that point is expected, valid state, not a bug.

## 2026-09-13 — `users.account_status` is the sole account-state field
**Decision:** `users.account_status` (`pending_verification`/`verified`/`suspended`/`deactivated`/`banned`) is the only field tracking account state. The `is_active`/`is_verified` booleans that had been added alongside it during M2 implementation are removed.
**Why:** Three overlapping fields for what is really one piece of state (e.g. `is_verified = true` while `account_status = suspended`) is a bug waiting to happen the first time one gets updated without the other. `account_status` alone matches what M1's stubbed `get_current_user` (`dependencies.py`) already expected to check.
**Impact:** `04_DATABASE.md` Cluster 1 `users` table gets the `account_status` column (with its CHECK constraint) added to the documented schema. `tenancy_identity/models.py` and its `UserResponse` schema drop `is_active`/`is_verified`.

## 2026-09-13 — Founding-Owner `Membership` defaults: open question, mechanical fix agreed
**Decision so far:** `Membership.is_owner` and `Membership.role` must not default to owner/business-executive at the column level — a column default applies to every future caller (e.g. M2's Invite Teammate flow), not just registration's founding membership. `is_owner` defaults to `false` at the column (matching `04_DATABASE.md`, which gives it no owner-leaning default either); `register()` must pass `is_owner=True` and an explicit `role` argument itself, rather than relying on the column.
**Open question, not yet resolved:** which `role` a founding Owner should hold (`hr_administrator` vs `business_executive` vs something else) — `08_DECISIONS.md`'s 2026-09-04-adjacent note already establishes `is_owner`/`role` as orthogonal axes (account authority vs product permissions), so this isn't answered by "they're the Owner." Needs a decision before `register()`'s `create_membership` call is finalized and before the next Alembic migration is generated.
**Impact:** `tenancy_identity/models.py`'s `Membership.role`/`is_owner` column defaults to be corrected; `auth/service.py`'s `register()` to pass both explicitly once the role question is settled.

## 2026-09-17 — Founding Owner's role resolved: `hr_administrator` (closes the 2026-09-13 open question)
**Decision:** `register()` always creates the founding Owner's `Membership` with `role=hr_administrator`, `is_owner=True`.
**Why:** `hr_administrator` is the broadest customer-side role already defined (`01_REQUIREMENTS.md` — HR Administrator is the system's primary operator persona, org/employee management, admin screens). Giving the founder that role, rather than `business_executive`, means they land with working access to every admin screen from day one, without needing a separate "Owner" role value that would duplicate permissions `hr_administrator` already carries. `is_owner` (account-level authority — can't be removed from the org, can't be deactivated) stays orthogonal to `role` (product-level permissions), matching the axis split this decision log already established.
**Known coupling, documented not fixed:** `POST /memberships` and `GET/PATCH /memberships` are gated on `require_org_role("hr_administrator")` (`tenancy_identity/router.py`) rather than on `is_owner` or a dedicated permission — this incidentally also covers the Owner today, since founders always hold that role, but would stop covering an Owner if a path is ever added for them to hold a different role. Flagged in-code; revisit if that path is ever built.
**Impact:** `tenancy_identity/models.py`'s `Membership.role`/`is_owner` defaults finalized; `auth/service.py`'s `register()` passes both explicitly. Closes the M2 blocker this decision log had open since 2026-09-13.

## 2026-09-17 — Invite-Teammate flow: existing users added immediately, unknown emails get a real Invite
**Decision:** `POST /memberships` (Invite Teammate) branches on whether the invited email already has an account: an existing user is added to the org immediately (new `Membership` row, no email round-trip needed); an email with no account gets a new `Invite` row (org, email, role, inviter, hashed token, expiry) and an emailed accept-link. A new `POST /auth/accept-invite` endpoint consumes that token, creates the `User` and `Membership` together, and marks the invite used.
**Why an `Invite` table, not reusing `EmailVerificationToken`:** an invite carries data a verification token doesn't need — which org, what role, who sent it — and must survive being looked up before any account (and thus any `user_id` to attach a token to) exists.
**Why no RLS on `Invite`:** same reasoning already established for `EmailVerificationToken`/`PasswordResetToken` — it's looked up by a raw bearer token before any tenant context can exist, so RLS scoped to `app.current_org_id` would block the one operation (`get_invite_by_token`) that has to work before that context is ever set.
**Module boundary preserved:** `generate_token`/`hash_token` moved from `auth/security.py` to a new `app/core/security.py`, and the invite-email Celery task lives in `tenancy_identity/tasks.py` (not `auth/tasks.py`) — both changes exist solely so `tenancy_identity` never has to import from `auth`, keeping the established one-directional module dependency (`auth` → `tenancy_identity`, never the reverse) intact.
**Impact:** `tenancy_identity/models.py` gets `Invite`; `repository.py`/`service.py` get `InviteRepository`/`MembershipService.invite_teammate`; `auth/service.py` gets `accept_invite`; `auth/router.py` gets `POST /auth/accept-invite`; `tenancy_identity/router.py` (previously empty) gets the full `POST/GET/PATCH /memberships` set. 14 new tests in `tests/memberships/`.

## 2026-09-17 — Live app DB role fixed: `elevare` (superuser) was being used for real traffic, RLS was never actually enforced
**Decision:** The live app (FastAPI, Celery worker, Celery Beat) now connects as `elevare_app`, a newly-provisioned ordinary Postgres role with no `SUPERUSER`/`BYPASSRLS` attributes (`backend/scripts/db/provision_app_role.sql`). `DATABASE_URL` points at it. A new `MIGRATION_DATABASE_URL` keeps the `elevare` superuser, used only by Alembic (`alembic/env.py`), which genuinely needs DDL privileges `elevare_app` deliberately lacks.
**What was actually wrong:** `docker-compose.yml`'s `POSTGRES_USER: elevare` makes `elevare` a Postgres superuser — that part is correct and unavoidable, every fresh Postgres cluster requires exactly one superuser at `initdb` time, regardless of what it's named. The bug was that `.env`'s `DATABASE_URL` also pointed the live app at that same superuser. Superusers (and any role with `BYPASSRLS`) skip RLS checks entirely, so every RLS policy documented since 2026-09-04 was installed correctly and syntactically fine, but had never actually been enforced against a real application request — for the whole of M1 and the portion of M2 leading up to this fix. Every earlier "proof" RLS was working was actually the app's own `WHERE` clauses happening to filter correctly, not Postgres enforcing anything.
**How it was caught:** a new test (`test_patch_membership_not_found_across_orgs`) returned 200 instead of the expected 404 — cross-org access that should have been invisible under RLS was succeeding. Confirmed via direct `pg_roles`/`pg_tables` queries that the app's own connection had `rolsuper=t`.
**Follow-on bug this exposed:** see next entry — `INSERT ... RETURNING` requires the read (`USING`) policy too, not just `WITH CHECK`. This only started failing once the app stopped running as a role that bypassed the check entirely.
**Impact:** `app/core/config.py` gets `migration_database_url`; `.env`/`.env.example` split `DATABASE_URL`/`MIGRATION_DATABASE_URL`; `alembic/env.py` uses the migration credential. `docs/RLS_POLICIES_EXPLAINED.md` §2 and `07_SECURITY.md`'s new provisioning checklist explain this for future reference. `docker-compose.yml` itself required no change — the fix is which credential each process is handed, not the container topology.

## 2026-09-17 — `INSERT ... RETURNING` needs the read policy satisfied too, not just the write policy
**Decision:** `OrganizationRepository.create_organization` now generates the new org's `id` in Python (`uuid.uuid4()`) before constructing the `Organization` object, and sets `app.current_org_id` to that exact id before the `INSERT` runs — rather than adding a `USING` exception to the `organizations` RLS policy.
**Why:** once the app stopped running as a superuser (previous entry), registration started failing with "new row violates row-level security policy for table organizations" even though `WITH CHECK`'s bootstrap branch should have allowed the insert. Root cause: the ORM's `flush()`/`refresh()` always issues `INSERT ... RETURNING` to read back server-generated columns (`created_at`); Postgres requires a `RETURNING`ed row to also satisfy the table's `USING` (read) policy, not just `WITH CHECK` — confirmed directly via `psql` as `elevare_app` (insert succeeds without `RETURNING`, fails with it). `organizations`' `USING` clause deliberately has no bootstrap exception (giving it one would mean any request that forgets to set org context could read every organization's row) — so establishing the real org context before the insert, rather than relaxing the read policy, closes the gap without reopening that hole.
**Correction to a prior claim:** this fix initially failed a different way — reading `new_organization.id` immediately after `Organization(**organization)` returned `None`/unset. `BaseModel.id`'s Python-side `default=uuid.uuid4` is only invoked at `flush()`, not at object construction, contrary to what had been assumed/stated earlier in this project. Fixed by generating the id explicitly before constructing the object.
**Impact:** `tenancy_identity/repository.py`'s `OrganizationRepository.create_organization`. `docs/RLS_POLICIES_EXPLAINED.md` §8 documents the general `RETURNING`-vs-`USING` mechanism for future tables that hit the same pattern.

## 2026-09-17 — `login()` now enforces `account_status`, closing a real pre-existing gap
**Decision:** `AuthService.login()` checks `account_status` (rejects `pending_verification`, `suspended`, `banned`, `deactivated`) before issuing tokens, mirroring the checks `get_current_user` already performed on every subsequent request.
**Why:** found while writing router-level tests — a deactivated/suspended/banned/unverified user could previously still complete login and receive a valid token pair; the account state was only ever checked on the *next* request. Login is the correct place to reject this outright, not just the requests after it.
**Impact:** `auth/service.py`'s `login()`. Required updating ~7 existing tests that had registered then logged in immediately without verifying email first (register's own immediate post-registration cookie had been masking that those tests weren't exercising login's real path).

## 2026-09-18 — M2 completeness audit: exception handling, resend-verification, stale RLS test suite fixed
**Decision:** Four fixes, from a structured completeness audit (per the "MVP means feature scope, not code quality" standard in `00_PROJECT_CONTEXT.md`), all verified by actually running the test suite, not assumed correct on read:
1. `register()`'s bare `except Exception: raise HTTPException(400, str(e))` replaced with a specific `except IntegrityError` (the real email-uniqueness race) mapped to `AlreadyExistsException`; anything else now propagates to the global handler instead of leaking raw error text as a fabricated 400.
2. `POST /auth/resend-verification` added — previously, a `PENDING_VERIFICATION` account with an expired/lost verification email had no way back in, a genuine lockout with no recovery path. Same anti-enumeration shape as `forgot_password`.
3. `tests/security/` (both RLS test files) had a hardcoded, stale connection string never updated to match real config — fixed to use `settings.database_url`, the same source every other test already uses.
4. `TABLES_WITHOUT_RLS`'s exclusion list was itself wrong in both directions: missing `alembic_version`/`refresh_tokens`/`email_verification_tokens`/`password_reset_tokens`/`invites` (all legitimately RLS-exempt, added since the list was first written), while incorrectly excluding `organizations` (which has a real policy and was never actually being checked). Corrected; the behavioral test's `SET LOCAL app.current_org_id = :param` also didn't work (Postgres doesn't accept bind parameters there) — fixed to use `set_config()`, matching the real application code, and rewritten to test `organizations`/`memberships` (the tables with real policies today) instead of a placeholder `departments` scenario two milestones away from existing.
**Also removed:** dead code (`validate_phone_digits` in `auth/schemas.py`, unused since it was written, confirmed via search before removal).
**Why this matters beyond the specific fixes:** the RLS test suite went from silently unable to run (wrong connection string) to actually passing but not actually checking most tables (wrong exclusion list) to now genuinely verifying real behavior — none of that would have surfaced without running it, not just reading it.
**Impact:** `auth/service.py`, `auth/router.py`, `auth/schemas.py`, `tests/security/test_row_level_security.py`, `tests/security/test_row_level_security_behavior.py`, new tests in `tests/authentication/test_auth_service.py` and `test_auth_router.py`. 72 tests passing (up from 68).

## 2026-09-18 — Membership deactivation scoped per-org, and two deeper bugs it uncovered by actually testing the fix
**Decision:** `PATCH /memberships/{id}` with `is_deactivated=True` now sets a new `Membership.deactivated_at` (nullable timestamp), not `User.account_status`. Deactivating someone from one organization no longer locks them out of every organization they belong to — `User.account_status` stays what it was, untouched by this endpoint. `get_current_membership` (`core/dependencies.py`) and `refresh()` now reject a deactivated membership the same way a missing one is rejected. `login()` filters to active (non-deactivated) memberships when picking which org to log into, and re-inviting a previously-deactivated person reactivates their existing row instead of failing on the unique constraint or creating a duplicate.
**Why scoped, not global:** raised as a direct question, confirmed by the user — a person in two unrelated organizations (a consultant invited into a client's org, say) must not have Org A's HR Administrator able to lock them out of Org B. `User.account_status` is deliberately global (2026-09-13's "sole account-state field" decision) for account-level concerns (banned, suspended platform-wide); membership-level access needed its own, separate flag.
**Two real, pre-existing bugs surfaced only by writing a real cross-org test and running it, not by reading the code:**
1. **`login()` never established real org context before relying on eager-loaded organization data.** It only "worked" in the existing test suite because the test environment's shared connection/transaction happened to leave a matching `app.current_org_id` behind from an earlier step (registration) in the same test. On a genuinely fresh connection (production), `organizations`' own RLS policy (no bootstrap-read exception at the time) would have silently returned no organization at all. Fixed: `login()` now determines the target org via the membership's plain `organization_id` column (no RLS needed to read a column on a row that already passed its own table's policy), explicitly sets `app.current_org_id` to that real value, then fetches the organization fresh.
2. **`organizations`' RLS policy had no bootstrap-read exception at all**, meaning `GET /me` could never actually return organization details for more than one membership — RLS only ever allows seeing one org's row at a time otherwise. Fixed by adding the same kind of bootstrap branch `memberships`' policy already had: when no org is chosen yet and the session is identified by `app.current_user_id`, a row is visible if it's an organization that user genuinely belongs to (via a subquery against `memberships`, itself already scoped the same way, so this can't leak organizations belonging to someone else). Read-only exception — `WITH CHECK` (writes) is unchanged.
3. **Both `login()` and `get_me()` now explicitly clear `app.current_org_id` before their own bootstrap logic runs**, rather than assuming it was never set. True in production (fresh transaction per request), but was an unenforced assumption, not a guarantee — worth hardening regardless of what exposed it.
**A new exception, `NoActiveMembershipException` (403), replaces reusing the generic `InternalServerErrorException` (500)** for "this user has zero active memberships anywhere" at login — deactivation makes this a normal, reachable outcome now, not the anomalous edge case (a brand-new user with literally zero memberships ever) the original 500 was written for.
**Impact:** `tenancy_identity/models.py` (`Membership.deactivated_at`, new migration), `tenancy_identity/service.py`, `core/dependencies.py`, `auth/service.py`, `core/exceptions.py` (`NoActiveMembershipException`), a new migration altering `organizations`' RLS policy, `docs/RLS_POLICIES_EXPLAINED.md` §5 rewritten, `04_DATABASE.md` Cluster 1 (Membership table + design note), tests in `tests/memberships/test_memberships.py` (one rewritten to actually reach zero-active-memberships via accept-invite rather than register, since a directly-registered user always has their own founding org as an unremovable extra membership; one new cross-org test). 73 tests passing.

## 2026-09-18 — `GET /memberships` paginated with `paginate()` (offset), not `paginate_cursor()`
**Decision:** `GET /memberships` now takes `page`/`limit` query params and returns the existing `PaginationResponse` envelope (`core/schemas.py`), via `core/pagination.py`'s offset-based `paginate()` — not the cursor-based `paginate_cursor()`, even though both already existed unused.
**Why offset, not cursor, for this specific endpoint:** three independent reasons, not a coin flip. (1) `paginate_cursor` is documented as being for "large, append-heavy result sets" (its own module docstring) — an org's membership list is a bounded admin roster (headcount-sized, rarely changes), the opposite shape. (2) `paginate_cursor` hardcodes newest-first ordering with no ascending option; this endpoint's existing contract is "oldest first" (founding/longest-tenured members surface first) — using it would have silently reversed that. `paginate()` doesn't impose its own ordering, so the existing `.order_by(created_at.asc())` survives untouched. (3) `paginate()`'s output shape already matched the pre-existing `PaginationMeta`/`PaginationResponse` schemas (page/limit/total/total_pages/has_next/has_previous) with zero new schema needed; `paginate_cursor` returns a different, cursor-shaped result with no matching schema anywhere yet, and no page numbers — suited to infinite-scroll UI, not an admin table. `paginate_cursor` is the right tool for something genuinely large and append-only later, e.g. `GET /audit-log` in M3.
**Also fixed alongside this:** an initial attempt at this endpoint's pagination (`get_org_memberships`) called `paginate_cursor` without importing it and discarded its return value, referencing an undefined `result` on the next line — would have raised `NameError` on every call. Caught before commit, never reached a real environment.
**Also fixed the same day:** `decode_refresh_token` (`auth/jwt_handler.py`) now wraps its `TokenPayload` construction in the same `try/except (KeyError, ValueError, TypeError) → TokenInvalidException` pattern `decode_access_token` already used — a malformed refresh-token payload previously raised a raw, unhandled `KeyError` (unstructured 500). New test coverage for the invite-reactivation branch (re-inviting a deactivated member reactivates the same membership row, doesn't duplicate it, restores login).
**Impact:** `tenancy_identity/repository.py`, `service.py`, `router.py` (pagination wiring), `auth/jwt_handler.py`, `tests/memberships/test_memberships.py` (two existing tests updated for the new response envelope, one new reactivation test). 74 tests passing, no schema drift.

## 2026-09-18 — M2 merged to `main`; frontend stack discrepancy surfaced (open, unconfirmed)
**Decision:** M2 backend merged to `main` via PRs #2 and #3. `m2-identity-auth` deleted (local + remote) per the standard workflow; `m3-audit-notifications` branched off the updated `main`.
**Open item, not yet resolved:** `00_PROJECT_CONTEXT.md` documents the frontend as "React (decoupled SPA)," but Uche has actually been building with Next.js. This isn't necessarily a conflict — it depends on whether he's using Next.js purely as a client-side toolchain (everything client-rendered, browser calls the API directly, matches the documented plan exactly) or using SSR/Server Components/Route Handlers for authenticated pages (which the current auth design doesn't account for: `localStorage` doesn't exist server-side, and a server-side proxy to the API wouldn't automatically carry the httpOnly refresh cookie the way direct browser-to-API calls do).
**Why this matters enough to log before an answer exists:** `03_ARCHITECTURE.md`'s auth token storage design (2026-09-07) was decided assuming direct browser-to-API calls under CORS with credentials — that assumption needs to hold for the design to still be correct, and nothing had ever confirmed it against what Uche was actually building until now.
**Resolved same day:** Uche confirmed everything is client-rendered — no SSR, no Server Components, no server-side proxy to the API. Browser will call the API directly, same as the documented plan; Next.js is being used purely as tooling. Deployment target (static export vs. running server, still undecided, likely Vercel) doesn't affect this either way since there's no server-side data fetching regardless of how it's deployed. **No architecture change needed** — this was a documentation wording gap, not a real conflict.
**One real, small action item surfaced by his question back:** he asked whether CORS already allows his origin with credentials — it does (`main.py`: explicit allow-list, `allow_credentials=True`, scoped methods/headers, all correct per `03_ARCHITECTURE.md`). The only gap is `CORS_ALLOWED_ORIGINS` (`.env`) is still `["http://localhost:5173"]`, a leftover placeholder from before this was confirmed. Deliberately not updated to a guessed `localhost:3000` — Emmanuel is hosting the frontend on Vercel, so the real fix is adding that actual URL once it exists, not a local dev port that's going away anyway.
**Impact:** `00_PROJECT_CONTEXT.md`'s "Frontend | React (decoupled SPA)" line should be updated to mention Next.js (cosmetic only, tech stack table). `backend/.env`'s `CORS_ALLOWED_ORIGINS` needs Uche's Vercel URL added once he has one — tracked, not urgent.
