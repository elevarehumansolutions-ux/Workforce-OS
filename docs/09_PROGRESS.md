# Progress

MVP milestone breakdown, dependency-ordered, no invented time estimates. Design and requirements are locked (`00`–`08`, no open scope conflicts as of 2026-09-07). Each milestone pairs its backend API surface with the frontend screens it unblocks, so Emmanuel and Uche can build side by side, matching the pairing already reflected on the Trello board.

**Explicitly excluded from every milestone below** (`01_REQUIREMENTS.md` "Explicitly Out of MVP Scope", `08_DECISIONS.md` 2026-09-03/04): Payroll (both layers) and Leave Management. Both are Phase 2. Leave Management's Phase-2 move is still unconfirmed with Jennifer, a product conversation, not a build-order question, and doesn't block this plan.

Ordering rule: a milestone is sequenced after every module it depends on per `02_SYSTEM_DESIGN.md`'s dependency graph, and infrastructure (Notifications, Audit Log, RLS/tenant context) lands before the first domain module that needs to call into it, not retrofitted later.

## Milestones

### M1 — Foundation & Infrastructure Scaffolding — ✅ DONE (2026-09-10)
Reviewed against `03_ARCHITECTURE.md`/`07_SECURITY.md`, no gaps found. See `08_DECISIONS.md` 2026-09-10. Repo initialized, committed (`439a9d2`).
**Backend:** Repo/module skeleton (`module_name/{models,repository,service,router}.py` per `02_SYSTEM_DESIGN.md`). Docker: one Dockerfile, three containers (`uvicorn`, `celery worker`, `celery beat`) per `03_ARCHITECTURE.md`. Postgres + Alembic. Redis. Celery wiring. FastAPI app entrypoint, router aggregation. CORS middleware (allow-list, `allow_credentials=True`). Base env config (`.env`, `.env.example`). The RLS regression test suite (`tests/security/test_row_level_security.py`, `test_row_level_security_behavior.py`) — written ahead of the schema it verifies, goes green as each milestone's tables land. **The `platform_admin` Postgres role** (`BYPASSRLS`, `07_SECURITY.md`) is also created here, alongside RLS itself — separate credential from the main app's `.env`, usable only from `scripts/admin/`, never the web/worker/beat containers. Provisioned this early because it's pure infra/security setup with no Billing-specific logic, and M3's Audit Log needs a real admin-script invocation path to log against.
**Frontend:** Vite/React app scaffold, API client, env config — parallel track, no shared blocker with backend yet.
**Depends on:** nothing.

### M2 — Identity, Auth & Multi-Tenancy — Backend ✅ DONE & MERGED to `main` (2026-09-18, PRs #2 and #3), Frontend not started
**Backend, shipped:** `organizations`/`users`/`memberships` (`04_DATABASE.md` Cluster 1, incl. `fiscal_year_start_month`), RLS policies on `organizations`/`memberships` (`docs/RLS_POLICIES_EXPLAINED.md`), the tenant-context FastAPI dependency chain (`get_current_user` → `get_current_membership` → `require_org_role`/`require_owner`, `core/dependencies.py`). All **eleven** auth endpoints (`register, verify-email, resend-verification, login, refresh, logout, change-password, forgot-password, reset-password, me, accept-invite`) plus the full membership set (`POST/GET/PATCH /memberships`) and the invite-teammate flow (new `Invite` model — see `08_DECISIONS.md` 2026-09-17). Bcrypt password hashing. JWT with `org_id`/`role` baked in, short-lived access + refresh, refresh re-validates live membership. 72 tests passing (`tests/authentication/`, `tests/memberships/`, `tests/security/`), all under a genuinely RLS-enforcing DB role (`elevare_app` — see 08_DECISIONS.md 2026-09-17, this was **not** true earlier in M2; the app had been silently running as a superuser that bypassed RLS entirely).
**Backend, completeness audit fixes (2026-09-18, see `08_DECISIONS.md`):** `register()`'s exception handling narrowed to the real failure mode instead of leaking raw errors as a fabricated 400. `POST /auth/resend-verification` added, closing a genuine account-lockout gap. `tests/security/`'s stale connection string and wrong exclusion list fixed — the suite now actually runs and actually checks every table, not just organizations/memberships. `validate_phone_digits` dead code removed. Membership deactivation rescoped from `User.account_status` (global) to `Membership.deactivated_at` (per-org) — the open product question below is now resolved and shipped, not just flagged. That fix's own testing surfaced and closed two deeper, pre-existing bugs: `login()` never established real org context before reading `Membership.organization` (worked only by test-environment coincidence, would have silently failed on a genuinely fresh production connection), and `organizations`' RLS policy had no bootstrap-read exception at all, meaning `GET /me` could never actually show org details for a multi-membership user. 73 tests passing.
**Backend, final cleanup (2026-09-18):** `decode_refresh_token` now wraps its `TokenPayload` construction in the same `try/except (KeyError, ValueError, TypeError) → TokenInvalidException` pattern `decode_access_token` already used, closing the raw-500-on-malformed-refresh-token gap. `GET /memberships` is now genuinely paginated (`page`/`limit` query params, `PaginationResponse` envelope) via the existing offset-based `paginate()` utility — see `08_DECISIONS.md` 2026-09-18 for why offset over cursor pagination specifically. New test covers the invite-reactivation branch (re-inviting a deactivated member reactivates the same row, doesn't duplicate, restores login). 74 tests passing.
**Backend, not yet done:** nothing outstanding on the backend side — all of the above, including the final cleanup fixes, is merged to `main`. `m2-identity-auth` deleted (local + remote) per the standard post-merge cleanup.
**Resolved (2026-09-18):** Uche's Next.js frontend confirmed client-rendered only, browser calls the API directly — matches the documented plan exactly, no architecture change needed. `00_PROJECT_CONTEXT.md`'s tech-stack line needs a wording update (mentions plain React, should note Next.js), purely cosmetic. See `08_DECISIONS.md`. One small pending action: `CORS_ALLOWED_ORIGINS` still has a placeholder value, needs Uche's real Vercel URL added once it exists.
**Frontend:** Registration (identity-only: name, work email, password), Login, token storage (access in `localStorage`, refresh as `httpOnly` cookie), org-switcher (for multi-membership `GET /me`), Invite Teammate screen (one-time onboarding action), and separately, the **Team Management screen** — ongoing view/manage of the org's people (`GET /memberships`, role change or deactivation via `PATCH /memberships/{id}`), a distinct, later-accessed screen from the onboarding invite flow. Not started this session.
**Depends on:** M1.

### M3 — Audit Log & Notifications (infrastructure modules) — Backend ✅ DONE (2026-09-20), Frontend not started
**Backend, shipped:** `audit_log`, `notifications` tables (`04_DATABASE.md` Clusters 8–9), RLS policies on both. Internal `log_action()` / `notify()` services — the ones every later domain module calls into (never commit themselves; caller's transaction owns durability — see `08_DECISIONS.md` 2026-09-20). All four endpoints: `GET /audit-log` (filters: `entity_type`, `entity_id`, `actor_user_id`, `date_from`, `date_to`; cursor-paginated via `paginate_cursor`, restricted to `hr_administrator`/`business_executive`), `GET /notifications` (filters: `category`, `unread=true`, self-scoped, any role), `POST /notifications/{id}/read`, `POST /notifications/read-all` (bulk mark-all-read). 16 new tests (`tests/audit_and_notification/`), 90 passing total. See `08_DECISIONS.md` 2026-09-20 for the full design writeup (`GET /audit-log`'s role gate, `notify()`'s list-of-recipients contract, the new `CursorPaginationResponse` envelope).
**Both gaps flagged in the 2026-09-18 review are closed:** `GET /audit-log`'s filtering/pagination, and bulk mark-all-read.
**Frontend:** Notification bell/center (role-filtered stream, category filter, mark-read). No audit log viewer screen (explicitly deferred, `07_SECURITY.md`). Not started.
**Depends on:** M2 (needs `actor_user_id`/`recipient_user_id`). Built now, ahead of the domain modules that call it, per the 2026-09-06 decision that uninstrumented write paths can't be retrofitted.


### M4 — Org Structure — Backend ✅ DONE & MERGED to `main` (2026-09-21, PR #7); Frontend not started
**Backend, shipped:** `locations`, `departments`, `positions`, `employees` (`04_DATABASE.md` Cluster 2, RLS policies on all four), full CRUD, `is_critical` toggle on departments, `reports_to_position_id` vs `employees.manager_id`, location filter + composite index, delete-blocked-while-referenced rule for `departments`/`positions` (named reason, both individual and combined-reason cases), audit-log calls on every mutation (`log_action()`, old/new diff in `changes`). **Employee offboarding: `POST /employees/{id}/offboard`** (not a generic status PATCH) — sets `employees.status = 'inactive'` and, if the employee has a `Membership`, deactivates it (`deactivated_at`) in the same transaction, both logged to `audit_log`. Does not block on the employee having direct reports (soft/reversible, not a delete — see `08_DECISIONS.md` 2026-09-20). **`POST /employees/{id}/reinstate`** — mirror action, reverses an offboard (`status = 'active'`, un-deactivates the `Membership`), reusing M2's existing invite-reactivation mechanism. Mutations restricted to `hr_administrator`, reads open to any authenticated org member (`08_DECISIONS.md` 2026-09-21). 25 new tests (`tests/organization/`), 115 passing total.
**Backend, not yet done:** nothing outstanding — merged to `main` via PR #7. `m4-org-structure` deleted (local + remote) per standard post-merge cleanup. See `08_DECISIONS.md` 2026-09-21 for a follow-up fix (`.gitignore`/provisioning-script commit) that got dropped by the PR merge and had to be recovered separately.

**Frontend:** Org setup screens — Departments (with Critical toggle), Positions/job architecture, Locations, Employee Directory + Add Employee (grant-login-access checkbox, Reporting Manager field, location filter), Offboard/Reinstate Employee actions. Trello card updated (`08_DECISIONS.md` 2026-09-20) to reflect the resolved offboard/reinstate design. Not started.
**Depends on:** M2, M3.
**Completeness gaps found in review (2026-09-18) — both resolved 2026-09-20, see `08_DECISIONS.md`:**
- Offboarding is a dedicated `POST /employees/{id}/offboard` action that also deactivates the linked `Membership` automatically, not a separate manual step — matches how real enterprise identity/HR systems (Okta Lifecycle Management, Workday leaver workflows) handle deprovisioning.
- Orphaned direct reports: offboarding does not block on having direct reports (`employees.manager_id` stays pointed at a real, if inactive, row — nothing is actually orphaned) and supports reversal via `POST /employees/{id}/reinstate`. This is distinct from, and doesn't change, the existing block-while-referenced rule for actually *deleting* a `positions` row (a real hard delete, still blocked).
- **Resolved 2026-09-21** (`08_DECISIONS.md`): the Business DNA field-list gap flagged since `01_REQUIREMENTS.md` §1 was first written is closed — see M5 below.

### M5 — Business DNA — Backend ✅ DONE & MERGED to `main` (2026-09-21, PR #10); Frontend not started
**Backend, shipped:** `business_dna`, `business_dna_core_values` (`04_DATABASE.md` Cluster 3, RLS on both). `GET|PUT /business-dna` (upsert), `POST|GET|PATCH|DELETE /business-dna/core-values`. Field set resolved 2026-09-21 (`08_DECISIONS.md`, `01_REQUIREMENTS.md` §1): business identity/industry/products & services, vision/mission/core values/business model, revenue/operational/customer value drivers, performance philosophy/workforce rules, capital investment amount. Does not re-ask org structure/job architecture (M4) or OKRs (M6) — those are captured by their own modules, not duplicated here.
**Access model (08_DECISIONS.md 2026-09-21):** reads open to any authenticated org member; mutations gated to `hr_administrator`; `capital_investment_amount` additionally field-restricted to `hr_administrator`/`business_executive` on reads (redacted, not endpoint-gated, for every other role). `organization_name` (still `NULL` post-registration since M2) is written through to `organizations.name` inside the same `PUT /business-dna` transaction, not a separate endpoint.
**Upsert logic:** create-or-update in one service call, matching the single `PUT` verb — a concurrent-create race (`organization_id` is `UNIQUE`) is recovered via a `SAVEPOINT` (`begin_nested()`), not a full `rollback()`; a full rollback was tried first and found, by actually running it, to silently wipe the transaction-scoped `app.current_org_id` RLS context set by the request dependency, breaking every later query in the same request.
**Response construction:** built field-by-field rather than `Response.model_validate(orm_object)` — `BusinessDNAResponse` has two fields (`core_values`, `organization_name`) that aren't plain columns, and letting Pydantic's `from_attributes` mode touch either via the ORM relationship on an async session risks the `MissingGreenlet` lazy-load crash M4 already hit once.
**Tests:** 11 new HTTP-level tests (`tests/business_dna/`) — auth/role gating, upsert round-trip with `organization_name` write-through, partial-update field preservation, the `capital_investment_amount` redaction for both a hidden (employee) and a visible (hr_administrator/business_executive) role, core-value CRUD including its own not-found/role-gating. 126 tests passing total.
**Frontend:** Business DNA onboarding step (the five-item field set above — company name captured here, not at registration). Not started.
**Depends on:** M2, M3.

### M6 — OKR — Backend ✅ DONE & MERGED to `main` (2026-09-21, PR #13); Frontend not started
**Backend, shipped:** `okrs`, `key_results` (Cluster 4, RLS via migration `80d102d22686`). Full CRUD, `department_id`/`location_id` scoping (null = corporate/dept-wide), both as `GET /okrs` filters. Mutations `hr_administrator`-only; reads open to any authenticated org member (`08_DECISIONS.md` 2026-09-21). Offset-paginated list endpoints, matching `GET /memberships`'s reasoning, not cursor. `OKRNotFoundException`/`KeyResultNotFoundException` added to `core/exceptions.py`. Every mutation audit-logged via `log_action()`. No `DELETE` endpoints — deferred to M8 in full alongside the `kpis.key_result_id` reference guard, a deliberate, time-boxed decision, not an oversight. 18 new tests (`tests/okrs/test_okrs_router.py`): CRUD, corporate/departmental/location scoping, both filters, role gating, 404s, the additive-not-replacing behavior, partial-update field preservation, cross-org RLS isolation for both OKRs and key results. 144 tests passing total (up from 126).
**Infrastructure bug found and fixed along the way (see `08_DECISIONS.md` 2026-09-21):** `alembic/env.py` never imported the model registry, so a bare `alembic revision --autogenerate` (exactly what `make migration` runs) silently produced a migration that dropped nearly every table in the schema instead of creating the new ones. Fixed at the source (`env.py` now imports `app.core.model_registry`) — every milestone from M7 on would otherwise have hit the same bug.
**Frontend:** OKR setup — corporate + departmental objectives/key results. Not started.
**Depends on:** M4 (department_id) — done.
**Pre-build decisions settled (2026-09-21, see `08_DECISIONS.md`):** mutations `hr_administrator`-only (Business Executive/Manager excluded, matching Org Structure/Business DNA precedent); `DELETE` deferred to M8 in full — no unguarded soft-delete in M6 either, since the thing a delete-block would check (`kpis.key_result_id`) doesn't exist as a table yet; `GET /okrs`/`GET /okrs/{id}/key-results` offset-paginated, not cursor; `location_id` filter added to `GET /okrs` alongside `department_id`. The 2026-09-18 gap (no delete-block rule for a referenced OKR) is closed by decision, not by code — it's now a stated, time-boxed limitation of the M6→M8 window, not an open question.
**Not yet done:** nothing outstanding on the backend side — merged to `main` via PR #13. `m6-okrs` deleted (local + remote) per the standard post-merge cleanup; `m7-ai-suggestions` branched off the updated `main`.

### M7 — AI Suggestions Engine (critical_position, revenue_allocation, missing_department)
**Backend:** `ai_suggestions`, `ai_usage_log` (Cluster 4). Shared internal LLM-calling utility (Claude, per `06_AI_DESIGN.md`), event-triggered Celery task fired from Org Structure/OKR services. Redis debounce (`NX EX 60`). Idempotent generation (partial unique index + `ON CONFLICT DO NOTHING`). ID-hallucination validation against real rows. `GET /ai-suggestions`, `POST /ai-suggestions/{id}/approve|reject`.
**Frontend:** AI Suggestions review screen — Approve/Edit/Reject for critical positions, revenue allocation (with running-total display), missing-department ("Add Department" action).
**Also here: the scheduled half of the "recurring, not one-time" requirement.** Event-triggered re-runs (a department marked critical, a new OKR saved) aren't the only trigger. The **Quarterly Objective Review** (`01_REQUIREMENTS.md` §4/workflow #4) is a separate, scheduled trigger, resolving what was an open gap during implementation planning (no fiscal-year field existed anywhere in `04_DATABASE.md` until 2026-09-07) — same shape as Task's Overdue check (`03_ARCHITECTURE.md`: "no user action to hang that check off of"), a Celery Beat job that reads each org's `organizations.fiscal_year_start_month` (added to Cluster 1, ships in M2, `DEFAULT 1` so calendar-year quarters work automatically for any org that doesn't customize it), scans for orgs whose fiscal quarter just ended, and re-runs suggestion generation additively.
**Depends on:** M2 (`organizations.fiscal_year_start_month`, for the scheduled trigger), M4, M6 (suggestions read department/position/OKR context — `06_AI_DESIGN.md`'s `missing_department` row specifically reads existing OKRs as prompt context, a read dependency, not a foreign key).
**Completeness gap found in review (2026-09-18), needs a decision:** nothing stops the exact same suggestion from being regenerated after HR explicitly rejects it. The idempotency fix (`08_DECISIONS.md` 2026-09-07) only prevents a *duplicate pending* row while one already exists, it says nothing about a *rejected* one. Given suggestions regenerate on every OKR/org-structure change and again every quarter (the Quarterly Objective Review above), a rejected suggestion could resurface repeatedly, indefinitely, training HR to ignore the whole review queue. Needs a decision before this milestone is built: skip generating a suggestion that matches an already-rejected one for the same target (permanently, or for some cooldown period), or leave it as-is and accept the repeat-nagging risk.

### M8 — Performance: KPI Definitions & Weighting
**Backend:** `kpis` (Cluster 5). `POST|GET|PATCH /kpis`, weight-sums-to-100-per-group validation (app-level, 409 on drift). Add `kpi_weight` as the fourth `ai_suggestions.suggestion_type`, reusing M7's engine. `GET /ai-usage` (endpoint ships here; its screen doesn't land until M13, merged with Billing History, not overlooked, just paired later).
**Frontend:** KPI setup per department, AI-suggested weight split shown in the same review pattern as M7. **Also here: assemble the full onboarding wizard** — this is the last module in the onboarding sequence, so it's where Business DNA (M5) → Org Setup (M4) → OKRs (M6) → AI Suggestions for Critical Roles (M7) → KPIs (this milestone) → Invite Team (M2) get stitched into one wizard with the corrected step order (`08_DECISIONS.md` 2026-09-02 — Critical Roles sit between OKRs and KPIs, not after both).
**Depends on:** M6 (`key_result_id`), M7 (kpi_weight suggestion reuses the engine).
**Completeness gap found in review (2026-09-18):** the weight-sums-to-100 validation checks a KPI being created or edited, but nothing described re-checks the *remaining* KPIs after one is deleted. Three KPIs at 30/30/40 losing the 40-weight one leaves 30/30 = 60%, silently under the required 100%, with nothing prompting a rebalance. Needs the same validation triggered on delete, not just create/edit.

### M9 — Attendance
**Backend:** `attendance_records` (Cluster 7). `POST /attendance/clock-in|clock-out`, `GET /attendance`. "Currently clocked in" derived (`clock_out_at IS NULL`).
**Frontend:** Employee clock-in/clock-out widget. Also removes the existing Morning/Evening/Night shift-scheduling grid UI from the Journey Walkthrough deck implementation — MVP attendance is plain clock-in/clock-out only (`08_DECISIONS.md` 2026-09-02).
**Depends on:** M4 (`employee_id`).
**Completeness gap found in review (2026-09-18), needs a decision:** nothing describes what happens if someone clocks in and never clocks out (forgets, or the tab just closes). "Currently clocked in" being derived from `clock_out_at IS NULL` means that record stays open indefinitely, silently pinned as "still clocked in" the next day, and needs a call on whether a second clock-in the next day is even allowed while one is still open, auto-close at some cutoff, block a new clock-in until the stale one is resolved, or something else. Also worth confirming double clock-in (clocking in again without clocking out first) is explicitly rejected, not silently creating two open records.

### M10 — Task
**Backend:** `tasks` (Cluster 6). Standalone task CRUD, `POST /tasks/{id}/complete`, KPI-weight inheritance (calls Performance's service), clock-in-gated task list (calls Attendance's service — task list only visible post-clock-in), overdue as derived state + Celery Beat scan that notifies assignee + department manager the moment `due_at` passes. Filters: department, assignee, status, kpi, overdue.
**Frontend:** Employee daily task list (post-clock-in gate, sortable High→Medium→Low), Assign Task screen (KPI link, due date), task detail/complete action.
**Depends on:** M8 (KPI weight lookup), M9 (clock-in check).
**Completeness gaps found in review (2026-09-18):**
- No cancel action. `tasks.status` includes `'cancelled'` in the schema, but `05_API_DESIGN.md` only lists `POST /tasks/{id}/complete`, no equivalent for closing a task that's no longer relevant (the underlying lead went cold, the request was withdrawn) without marking it `completed`, which would incorrectly count toward positive KPI scoring it never actually earned.
- No reassignment endpoint described. A task's `assigned_to_employee_id` can't be changed after creation via anything in `05_API_DESIGN.md` — worth confirming this is meant to go through the generic task update, or needs its own explicit action.

### M11 — Workflow
**Backend:** `workflow_templates`, `workflow_steps`, `workflow_instances` (Cluster 6). Template/step CRUD (HR Administrator or any Manager), `POST /workflow-instances` (start a case), completing a step's task auto-creates the next step's task via Task's service (never a direct `tasks` insert), default-assignee-or-manager-delegation routing, same department allowed to repeat across steps.
**Frontend:** Workflow Template builder (ordered steps, department, turnaround, optional default assignee), Start Workflow Instance screen, instance status/progress view.
**Depends on:** M10 (Workflow only ever calls Task's service, never the other way).
**Completeness gaps found in review (2026-09-18):**
- No cancel action for a running instance. `workflow_instances.status` includes `'cancelled'` in the schema, but no endpoint for it is described, needed for the ordinary real-world case of an abandoned process (a client backs out mid-onboarding, say).
- **Needs a decision:** if a `workflow_template` gets edited (a step's turnaround time changed, a step added or removed) while instances are actively running against it, do those in-flight instances keep using the template as it was when they started (a snapshot), or always reflect the latest edit? Nothing states this either way, and it changes real behavior, an in-flight instance's remaining deadlines could shift out from under it if edits apply retroactively.

### M12 — Performance: Scoring, Dashboards & Executive Intelligence
**Backend:** `kpi_scores` recording (`POST /kpis/{id}/scores`, `score_percentage` calc incl. inverse KPIs), `GET /departments/{id}/performance-summary` (quarterly revenue-target rollup), `employee_kpi_scores` (Cluster 5 — computed from each employee's tasks linked to a KPI, `due_at`/`completed_at` timeliness), `GET /employees/leaderboard`, `GET /company-performance-summary` (Company Performance Score, quarterly trend + custom range, Most Improved), `executive_summaries` + Celery Beat LLM narrative generation triggered on period recalculation.
**Frontend:** Manager/Executive dashboards — quarterly trend graph with custom date range, department drill-down to individual employees, employee leaderboard, Company Performance Score headline, Most Improved callout, AI-generated executive summary block. These are the investor-facing dashboard additions from `08_DECISIONS.md` 2026-09-07, treat visual polish here as first-class, not an afterthought.
**Depends on:** M10 (task timeliness data is the actual input to `employee_kpi_scores`), M11 (workflow-generated tasks also feed scoring).
**Worth stating explicitly before this is built (2026-09-18 review):** if a KPI's weight changes after `kpi_scores` for past periods already exist, those historical scores should stay exactly as originally computed, not get silently recalculated against the new weight. This already follows from `04_DATABASE.md`'s design note that `kpi_scores` is deliberately not soft-deletable, a closed period is a permanent historical fact, but it's worth restating here since this milestone is where that principle actually gets exercised in code for the first time.

### M13 — Billing & Platform Admin
**Backend:** `billing_records`, `organizations.subscription_status`/`subscription_expires_at` (Cluster 1). `GET /billing/history` (Owner/HR Administrator). Uses the `platform_admin` (`BYPASSRLS`) role and `scripts/admin/` already provisioned in M1 to set these fields manually — no role creation here, this milestone only adds the billing-specific admin scripts and the read endpoint. Every invocation audit-logged (M3).
**Frontend:** **Admin Settings screen — Billing History + AI Usage combined**, one settings area, not two separate pages (`GET /billing/history` from this milestone, `GET /ai-usage` shipped back in M8). Owner/HR Administrator only, read-only, no payment page, no checkout, Paystack/Flutterwave handle the actual invoice and payment.
**Depends on:** M2 (organizations), M3 (audit log for admin-script invocations), M8 (`GET /ai-usage` already exists by this point).

### M14 — Security Hardening
**Backend:** Rate limiting on `/auth/login` and `/auth/refresh` (`slowapi` or reverse-proxy layer). CORS allow-list finalized for real staging/prod origins. Secrets audit (`.env`/`.mcp.json` never committed, rotation check for anything ever exposed). HTTPS/TLS enforced past local dev. NDPA data-protection flag surfaced as an explicit "needs legal sign-off" checklist item, not silently treated as compliant.
**Frontend:** Surface rate-limit/lockout error states on login.
**Depends on:** M2 (auth endpoints exist to rate-limit).
**Process note from the 2026-09-18 review, worth checking here explicitly:** `tests/security/`'s RLS suite checks every table dynamically, but its exclusion list (`TABLES_WITHOUT_RLS`) is manually maintained, and it already went stale once (2026-09-18, missing several tables that existed by then). By M14, M4 through M12 will have added roughly a dozen new tables, some of which may legitimately need RLS exemptions of their own (bearer-token-lookup tables, if any more get added). Part of this milestone should be re-auditing that exclusion list against the schema as it actually stands by then, not assuming it was kept current along the way.

### M15 — Deployment & Demo Readiness
**Backend:** `docker-compose.prod.yml`, hosting deploy (Railway dev / Azure prod per `00_PROJECT_CONTEXT.md`, pending CloudSA conversation), seed script populating Elevare itself as tenant #1 (real departments, OKRs, critical roles, KPIs) as demo data, end-to-end smoke test across onboarding → daily execution → dashboards.
**Frontend:** Production build/deploy, final polish pass specifically on investor-facing screens (executive dashboard, AI suggestion review, onboarding wizard) — this MVP is demoed to investors and public figures per project context.
**Depends on:** all prior milestones.
**Completeness gaps found in review (2026-09-18):**
- The seed script needs to produce *historical* data spanning multiple past periods, not just current-state rows. `01_REQUIREMENTS.md` §11's quarterly trend graph, the Company Performance Score, and the leaderboard (`08_DECISIONS.md` 2026-09-07, explicitly built for investor-facing polish) all need `kpi_scores`/`employee_kpi_scores` across several past quarters to render as anything but flat or empty. A seed script that only populates today's departments/OKRs/KPIs with no scoring history would demo badly on exactly the screens this MVP is supposed to be showing off.
- Nothing anywhere confirms database backups are actually enabled on whichever hosting gets chosen (Railway dev / Azure prod, per `00_PROJECT_CONTEXT.md`). `07_SECURITY.md` names this as an operational item but doesn't assign it anywhere, this milestone, being the actual deployment step, is where it needs to land, checked and confirmed, not assumed.

## Trello cross-check (2026-09-07, actioned same day)

Cross-checked this plan against the "Elevare Workforce OS" Trello board (To Do / Doing / Done). Three gaps found, all now actioned directly on the board, not just noted here:
- **Duplicate "[Backend] Audit Log module" card** (positions 1500 and 3500, same list) — the newer one (created a day after the original, no comments/checklists on either) archived. The original at position 1500 stands.
- **"[Backend] Notifications module" card was queued at position 23000** in To Do, right before Workflow's frontend and the Executive dashboard card — far later than Audit Log (1500), despite `02_SYSTEM_DESIGN.md` treating both as the same category of early infrastructure and M3 above bundling them together. Left as-is, M7 (AI Suggestions, resurfaces via notifications) and M11 (Workflow approvals) would reach a queue position for a service that hadn't been started yet. **Moved to position 1600**, right after Audit Log, matching M3's build order.
- **M14 (Security Hardening) and M15 (Deployment & Demo Readiness) had no matching cards.** Added: `[Backend] Security Hardening`, `[Backend] Deployment & Demo Readiness`, `[Frontend - Uche] Production build & investor-facing polish pass`, all in To Do at positions 26000–28000, after the existing Executive dashboard card.

## Engineering hygiene — Ruff docstring compliance pass (2026-09-21)

Cross-cutting, not tied to one milestone. Full write-up in `08_DECISIONS.md` 2026-09-21. `backend/pyproject.toml` added (ruff `D`/pydocstyle rules, Google convention); every `.py` file under `backend/app/`, `backend/alembic/`, `backend/tests/` brought to zero ruff errors. 115/115 tests still passing.

**Completeness gap found, not yet closed (required-for-completeness, not nice-to-have):** `backend/Makefile`'s `lint` target (`ruff check . && black --check . && isort --check-only .`) has apparently never been runnable — none of `ruff`, `black`, or `isort` are in `backend/requirements.txt`, so the container `make lint` execs into doesn't have them installed. Needs a decision on which of these stay (ruff alone may be enough — it can replace both black's check and isort's role via `ruff format --check`/import-sorting rules) before pinning versions and adding them to `requirements.txt`. M14 (Security Hardening) is the natural place to close this, given it's already scoped as the milestone that finalizes hardening/process items like `07_SECURITY.md`'s operational checklist.
