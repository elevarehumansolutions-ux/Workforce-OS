"""Domain modules for the Elevare Workforce OS backend.

Each submodule (auth, tenancy_identity, organization, audit_and_notification,
business_dna, ...) owns its own models, schemas, repository, service, and
router for a single bounded area of the platform.
"""
